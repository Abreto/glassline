{
  "name": "glassline",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only local AI agent session viewer.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "start": "node src/server.mjs",
    "install-launchd": "node scripts/install-launchd.mjs",
    "uninstall-launchd": "node scripts/uninstall-launchd.mjs",
    "test": "node --test",
    "check:sensitive-history": "node scripts/check-sensitive-history.mjs",
    "release-check": "npm test && npm run check:sensitive-history"
  },
  "engines": {
    "node": ">=20"
  }
}
